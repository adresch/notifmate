<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotifMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="backgroundColor">@color/black</item>
        <item name="lightBlack_darkWhite">@color/white</item>
        <item name="toplogo">@drawable/logo_dark__4_</item>
        <item name="progressColor">#393636</item>
        <item name="day_night_pause">@drawable/icons8_pause_button_48___white</item>
        <item name="splash_logo">@drawable/splash_dark</item>
    </style>

    <style name="Theme.NotifMate" parent="Base.Theme.NotifMate" />

    <style name="Theme.RemoveSplashScreenTheme" parent="@style/Theme.NotifMate">
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>